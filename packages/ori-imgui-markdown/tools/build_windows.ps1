# Phase OS deferred — Linux maturity 5 complete (0.1.0)
# Real MSVC build / IMM32 IME is out of scope until Phase OS (see ori-lang docs/planning/PHASE-OS.md).
Write-Host "Phase OS deferred — Linux only / multi-OS last (maturity 5 does not require Win/mac)"
Write-Host "IME: real IMM32 is Windows-only; Linux ships markdown.ime_stub no-ops"
exit 0
